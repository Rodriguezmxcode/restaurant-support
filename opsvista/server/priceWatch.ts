import { getRestaurant365Credentials, getIntegrationSnapshot, saveIntegrationSnapshot, type Restaurant365Credentials } from './integrationStore.js';
import { analyzePriceWatch, classifyPriceWatchItem, type PriceWatchLine, type PriceWatchResponse } from '../shared/priceWatch.js';
type Row=Record<string,unknown>;const baseUrl=()=> (process.env.RESTAURANT365_ODATA_BASE_URL?.trim()||'https://odata.restaurant365.net/api/v2/views').replace(/\/$/,'');
const pick=(row:Row,names:string[])=>{for(const name of names){const entry=Object.entries(row).find(([key])=>key.toLowerCase()===name.toLowerCase());if(entry&&entry[1]!==undefined&&entry[1]!==null&&String(entry[1]).trim()!=='')return entry[1];}return undefined;};const text=(row:Row,names:string[])=>{const v=pick(row,names);return v===undefined?'':String(v).trim();};const num=(row:Row,names:string[])=>{const v=pick(row,names);if(v===undefined||v===null||String(v).trim()==='')return null;if(typeof v==='number'&&Number.isFinite(v))return v;const raw=String(v).trim(),negative=/^\(.*\)$/.test(raw),n=Number(raw.replace(/[$,()\s]/g,''));return Number.isFinite(n)?(negative?-Math.abs(n):n):null;};const bool=(row:Row,names:string[])=>{const v=pick(row,names);return v===true||v===1||['true','1','yes'].includes(String(v).toLowerCase());};const id=(row:Row)=>text(row,['transactionId','id']).toLowerCase();const validDate=(v:string)=>/^\d{4}-\d{2}-\d{2}$/.test(v)&&!Number.isNaN(Date.parse(`${v}T12:00:00Z`));const days=(s:string,e:string)=>Math.floor((Date.parse(`${e}T00:00:00Z`)-Date.parse(`${s}T00:00:00Z`))/86400000)+1;
async function credentials(org:string){const stored=await getRestaurant365Credentials(org);if(stored)return stored;const domain=process.env.RESTAURANT365_DOMAIN?.trim(),username=process.env.RESTAURANT365_USERNAME?.trim(),password=process.env.RESTAURANT365_PASSWORD;if(domain&&username&&password)return{domain,username,password} as Restaurant365Credentials;throw new Error('Restaurant365 no está conectado.');}function rows(payload:unknown):Row[]{if(Array.isArray(payload))return payload.filter(v=>v&&typeof v==='object') as Row[];if(payload&&typeof payload==='object'&&Array.isArray((payload as {value?:unknown}).value))return ((payload as {value:unknown[]}).value).filter(v=>v&&typeof v==='object') as Row[];return[];}async function odata(c:Restaurant365Credentials,view:string,params:Record<string,string>){const url=new URL(`${baseUrl()}/${view}`);for(const [k,v] of Object.entries(params))url.searchParams.set(k,v);const auth=Buffer.from(`${c.domain}\\${c.username}:${c.password}`,'utf8').toString('base64');const response=await fetch(url,{headers:{Accept:'application/json',Authorization:`Basic ${auth}`},signal:AbortSignal.timeout(20000)});if(!response.ok){if([401,403].includes(response.status))throw new Error('Restaurant365 rechazó las credenciales o permisos para Price Watch.');if(response.status===429)throw new Error('Restaurant365 limitó temporalmente las consultas de Price Watch.');throw new Error(`${view}: Restaurant365 respondió ${response.status}.`);}return rows(await response.json());}async function all(c:Restaurant365Credentials,view:string,params:Record<string,string>,max=20000,pageSize=250){const out:Row[]=[];for(let skip=0;skip<max;skip+=pageSize){const page=await odata(c,view,{...params,'$top':String(pageSize),'$skip':String(skip)});out.push(...page);if(page.length<pageSize)return out;}throw new Error(`${view}: Price Watch alcanzó el límite de paginación.`);}function chunks<T>(v:T[],s:number){const out:T[][]=[];for(let i=0;i<v.length;i+=s)out.push(v.slice(i,i+s));return out;}async function parallel<T,R>(values:T[],limit:number,fn:(v:T)=>Promise<R>){const out=new Array<R>(values.length);let cursor=0;const worker=async()=>{while(cursor<values.length){const i=cursor++;out[i]=await fn(values[i]);}};await Promise.all(Array.from({length:Math.min(limit,values.length)},worker));return out;}const normalizeLocation=(v:string)=>{const n=v.toLowerCase();return ['Stamford','Orange','Fairfield','Danbury','Avon','Southington'].find(x=>n.includes(x.toLowerCase()))||v||'Sin locación';};
const performanceSkirt:Record<string,{name:string;invoiceDescription:string}>={
 '271972':{name:'BEEF Skirt Premium · Cortes',invoiceDescription:'PACKER BEEF SKIRT OUTSIDE BNLS P'},
 '283558':{name:'Carne Asada · Tacos (Beef Skirt)',invoiceDescription:'PACKER BEEF SKIRT OUTSIDE FRSH'},
};
const performanceVendor=(vendor:string)=>/performance\s*food/i.test(vendor);
const chefsWarehouseVendor=(vendor:string)=>/chef'?s?\s*warehouse|chefs\s*warehouse/i.test(vendor);
const chefsWarehouseSeabassSku=(detail:Row,vendor:string,rawName:string)=>{if(!chefsWarehouseVendor(vendor)||!/seabass/i.test(rawName))return'';const candidate=text(detail,['vendorItemNumber','vendorItemNo','productCode','sku','brandItemNumber','itemNumber']);return /^\d{8}-\d{2}-\d{3}$/.test(candidate)?candidate:'';};
const normalizeKnownProductName=(name:string,vendor:string)=>{const n=name.toLowerCase();if(chefsWarehouseVendor(vendor)&&/wagyu\s+burger\s+blend.*8\s*oz.*frz/.test(n))return'BEEF Wagyu · Wagyu Burger Blend 8oz FRZ';return name;};
const knownPerformanceSku=(detail:Row,vendor:string)=>{if(!performanceVendor(vendor))return'';const candidate=text(detail,['vendorItemNumber','vendorItemNo','productCode','purchasedItemNumber','inventoryItemNumber','sku','brandItemNumber','itemNumber']);return performanceSkirt[candidate]?candidate:'';};
type ItemLookup={name:string;number?:string};function parseLine(detail:Row,header:Row,vendor:string,itemLookup:Map<string,ItemLookup>):PriceWatchLine|null{const itemId=text(detail,['itemId','item','purchasedItemId','inventoryItemId','vendorItemId']);const catalog=itemId?itemLookup.get(itemId.toLowerCase()):undefined;const rawName=text(detail,['itemName','purchasedItemName','inventoryItemName','vendorItemName','vendorItem','description']);const knownSku=knownPerformanceSku(detail,vendor);const knownProduct=knownSku?performanceSkirt[knownSku]:undefined;const catalogName=rawName&&!/^art[ií]culo sin nombre$/i.test(rawName)?rawName:(catalog?.name||'');const seabassSku=chefsWarehouseSeabassSku(detail,vendor,catalogName);const itemName=knownProduct?.name||normalizeKnownProductName(catalogName,vendor);const vendorItemNumber=knownSku||seabassSku||text(detail,['vendorItemNumber','vendorItemNo','productCode','purchasedItemNumber','inventoryItemNumber','sku','brandItemNumber'])||'';const rawItemNumber=text(detail,['itemNumber']);const r365ItemNumber=catalog?.number||(knownSku&&rawItemNumber===knownSku?'':rawItemNumber)||'';const quantity=num(detail,['quantity','qty','invoiceQuantity','receivedQuantity']);const uom=text(detail,['unitOfMeasure','unitOfMeasureName','uom','purchaseUnitOfMeasure','purchaseUom','unitName']);const directUnit=num(detail,['eachAmount','unitPrice','price','amountEach','costEach','invoiceUnitPrice']);const rawTotal=num(detail,['lineAmount','lineTotal','extendedPrice','extPrice','total','amount','debit']);const lineTotal=rawTotal===null?null:Math.abs(rawTotal);const unitPrice=directUnit!==null?Math.abs(directUnit):quantity!==null&&quantity!==0&&lineTotal!==null?Math.abs(lineTotal/quantity):null;const packSize=text(detail,['packSize','caseSize','packageSize','size','purchasePackSize','casePack','packCount','unitsPerCase','unitCount','caseQuantity']);const weight=num(detail,['weight','catchWeight','actualWeight']);if(!itemName&&!vendorItemNumber&&!itemId)return null;const verify:string[]=[];if(quantity===null||quantity===0)verify.push('Cantidad sin confirmar');if(!uom)verify.push('UOM sin confirmar');if(unitPrice===null||unitPrice<=0)verify.push('Precio unitario sin confirmar');if(!itemName)verify.push('Nombre de producto sin confirmar');if(!vendorItemNumber&&!itemId)verify.push('PRODUCT IDENTITY: falta R365 Item ID o Vendor SKU');const type=text(header,['type']),credit=/credit/i.test(type),date=text(header,['date']).slice(0,10),location=normalizeLocation(text(header,['locationName','location'])),name=itemName||r365ItemNumber||vendorItemNumber||`R365 Item ${itemId.slice(0,8)}`;return{transactionId:id(header),invoiceNumber:text(header,['transactionNumber','number'])||undefined,date,location,vendor,approved:bool(header,['isApproved','approved']),credit,itemId:itemId||undefined,itemName:name,r365ItemNumber:r365ItemNumber||undefined,vendorItemNumber:vendorItemNumber||undefined,uom:uom||undefined,quantity:quantity===null?null:Math.abs(quantity),unitPrice:unitPrice!==null&&unitPrice>0?unitPrice:null,lineTotal,packSize:packSize||undefined,weight,category:classifyPriceWatchItem(name,vendor,uom),source:'r365-item',verify};}
export async function getPriceWatch(organizationId:string,start:string,end:string,refresh=false):Promise<PriceWatchResponse>{if(!validDate(start)||!validDate(end)||start>end||days(start,end)>93)throw new Error('Price Watch requiere un rango válido de hasta 93 días.');const key=`price-watch-v12:${start}:${end}`;if(!refresh){const saved=await getIntegrationSnapshot<PriceWatchResponse>(organizationId,'restaurant365-price-watch',key);if(saved&&Date.now()-Date.parse(saved.updatedAt)<30*60*1000)return saved.payload;}const c=await credentials(organizationId),endExclusive=new Date(Date.parse(`${end}T00:00:00Z`)+86400000).toISOString().slice(0,10);const headers=await all(c,'Transaction',{'$select':'transactionId,locationId,locationName,date,transactionNumber,type,isApproved,companyId,name','$filter':`date ge ${start}T00:00:00Z and date lt ${endExclusive}T00:00:00Z`},20000,250);const invoices=headers.filter(row=>/^ap\s*(invoice|credit(?:\s*memo)?)$/i.test(text(row,['type']).trim()));const companyIds=[...new Set(invoices.map(row=>text(row,['companyId'])).filter(Boolean))],companies=new Map<string,string>();for(const batch of chunks(companyIds,20)){const result=await all(c,'Company',{'$select':'companyId,name','$filter':batch.map(value=>`companyId eq ${value}`).join(' or ')},5000,250).catch(()=>[]);for(const row of result)companies.set(text(row,['companyId']).toLowerCase(),text(row,['name'])||'Proveedor sin identificar');}const batches=chunks(invoices.map(id).filter(Boolean),8),pages=await parallel(batches,2,async batch=>all(c,'TransactionDetail',{'$filter':batch.map(value=>`transactionId eq ${value}`).join(' or ')},5000,250).catch(async()=>{const singles=await parallel(batch,4,value=>all(c,'TransactionDetail',{'$filter':`transactionId eq ${value}`},2000,250));return singles.flat();})),details=pages.flat(),byId=new Map<string,Row[]>();for(const row of details){const key=id(row),list=byId.get(key)||[];list.push(row);byId.set(key,list);}const itemIds=[...new Set(details.map(r=>text(r,['itemId','item','purchasedItemId','inventoryItemId','vendorItemId'])).filter(Boolean))],itemLookup=new Map<string,ItemLookup>();for(const batch of chunks(itemIds,20)){for(const view of ['PurchasedItem','Item','InventoryItem']){const found=await all(c,view,{'$filter':batch.map(value=>`${view==='PurchasedItem'?'purchasedItemId':view==='InventoryItem'?'inventoryItemId':'itemId'} eq ${value}`).join(' or ')},5000,250).catch(()=>[]);for(const row of found){const iid=text(row,['purchasedItemId','inventoryItemId','itemId','id']);if(iid&&!itemLookup.has(iid.toLowerCase()))itemLookup.set(iid.toLowerCase(),{name:text(row,['name','itemName','description','purchasedItemName','itemNumber'])||`R365 Item ${iid.slice(0,8)}`,number:text(row,['itemNumber','number','purchasedItemNumber','inventoryItemNumber','sku','vendorItemNumber'])||undefined});}if(found.length)break;}}const parsed:PriceWatchLine[]=[];let accountLines=0;for(const header of invoices){const vendor=companies.get(text(header,['companyId']).toLowerCase())||text(header,['name'])||'Proveedor sin identificar';for(const detail of byId.get(id(header))||[]){const line=parseLine(detail,header,vendor,itemLookup);if(line)parsed.push(line);else accountLines++;}}const result=analyzePriceWatch(parsed,start,end);result.sourceCoverage.accountLines=accountLines;result.sourceCoverage.invoices=invoices.length;result.caveats=['Price Watch prioriza Vendor Item/SKU cuando existe, porque un mismo R365 Purchased Item puede contener variantes del proveedor. Para Chefs Warehouse, los Vendor Item # de SEAFOOD Seabass se mantienen separados aunque compartan el mismo R365 Item. Para Performance Food Service, SKU 271972 se clasifica como BEEF Skirt Premium · Cortes y SKU 283558 como Carne Asada · Tacos (Beef Skirt), según el invoice validado del 18-Sep-2026.','Productos con nombres iguales pero vendors distintos —por ejemplo Lucero Produce vs The Spice Mill— quedan separados salvo que Restaurant365 confirme el mismo Item ID.','Cases con peso conocido se normalizan a $/lb. Si R365 devuelve cantidad en libras aunque el UOM diga case, Price Watch detecta múltiplos del peso del case para evitar dividir dos veces.','Ejemplo: SEAFOOD Shrimp 26-30, case 6 × 4 LB a $214.80 = $8.95/lb; compras registradas a $8.95/lb deben quedar estables, no CRITICAL.'];await saveIntegrationSnapshot(organizationId,'restaurant365-price-watch',key,result);return result;}
